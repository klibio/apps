<?xml version="1.0" encoding="utf-8"?>
<configuration>

	<!-- Define default property -->
	<property name="LOG_PATH" value="${logging.root:-.}" />

	<!-- Create appenders -->
	<appender name="STDOUT"
		class="ch.qos.logback.core.ConsoleAppender">
		<filter class="ch.qos.logback.classic.filter.LevelFilter">
			<level>ERROR</level>
			<onMatch>ACCEPT</onMatch>
			<onMismatch>NEUTRAL</onMismatch>
		</filter>
		<filter class="ch.qos.logback.classic.filter.LevelFilter">
			<level>INFO</level>
			<onMatch>ACCEPT</onMatch>
			<onMismatch>DENY</onMismatch>
		</filter>
		<encoder>
			<pattern>%date{ISO8601} [%level] %logger - %msg%n</pattern>
		</encoder>
	</appender>

	<appender name="FILE"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${LOG_PATH}/pdfgen.log</file>
		<append>true</append>
		<rollingPolicy
			class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
			<fileNamePattern>${LOG_PATH}/pdfgen%i.log.zip</fileNamePattern>
			<minIndex>1</minIndex>
			<maxIndex>10</maxIndex>
		</rollingPolicy>
		<triggeringPolicy
			class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
			<maxFileSize>1MB</maxFileSize>
		</triggeringPolicy>
		<encoder>
			<pattern>%date{ISO8601} [%thread] %-5level %logger - %msg%n</pattern>
		</encoder>
	</appender>

	<!-- Hide all messages from logback itself -->
	<logger name="ch.qos.logback.classic.util" level="OFF" />
	<logger name="ch.qos.logback.core.model.processor" level="OFF" />
	<logger name="ch.qos.logback.core" level="OFF" />
	<logger name="org.apache.fop.fonts.truetype" level="OFF" />
	<!--
	<logger name="org.apache.fop.apps" level="OFF" />
-->
	<!-- Assign loggers to appenders -->
	<root>
		<appender-ref ref="STDOUT" />
		<!--
		<appender-ref ref="FILE" />
-->
	</root>
</configuration>
