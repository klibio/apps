<?xml version="1.0" encoding="utf-8"?>
<configuration>

	<!-- Define default property -->
	<property name="LOG_PATH" value="${logging.root:-.}" />

	<!-- Create appenders -->
	<appender name="STDOUT"
		class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>%date{HH:mm:ss.SSS} [%-5level] [%logger{25}] # %msg%n</pattern>
			<!-- 
				<pattern>%date{ISO8601} [%level] %logger - %msg%n</pattern>
				<pattern>%date{HH:mm:ss.SSS} [%.20thread] [%-5level] %logger{30} - %msg%n</pattern> 
			-->
		</encoder>
	</appender>

	<appender name="FILE"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${LOG_PATH}/pdfgen_debugmode.log</file>
		<append>true</append>
		<rollingPolicy
			class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
			<fileNamePattern>${LOG_PATH}/pdfgen_debugmode%i.log.zip
			</fileNamePattern>
			<minIndex>1</minIndex>
			<maxIndex>10</maxIndex>
		</rollingPolicy>
		<triggeringPolicy
			class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
			<maxFileSize>10MB</maxFileSize>
		</triggeringPolicy>
		<encoder>
			<pattern>%date{ISO8601} [%thread] %-5level %logger - %msg%n</pattern>
		</encoder>
	</appender>

	<!-- Assign loggers to appenders -->
	<root>
		<appender-ref ref="STDOUT" />
		<!--
		<appender-ref ref="FILE" />
-->
	</root>
</configuration>
